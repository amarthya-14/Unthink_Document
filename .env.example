# Preferred provider, tried first: "gemini" (default, recommended) or "groq".
# You can set BOTH keys below — if the preferred provider fails for any
# reason (bad key, rate limit, model retired), the app automatically falls
# back to the other one instead of showing an error.
VITE_AI_PROVIDER=gemini

# Gemini — get a free key at https://aistudio.google.com/apikey (no card required)
VITE_GEMINI_API_KEY=your_gemini_api_key_here

# Optional: force a specific Gemini model if auto-discovery picks the wrong one.
# VITE_GEMINI_MODEL=gemini-3.6-flash

# Groq — used as the fallback ("basic") provider if Gemini fails, or as the
# primary if VITE_AI_PROVIDER=groq. Free key at https://console.groq.com/keys
VITE_GROQ_API_KEY=your_groq_api_key_here

# Optional: force a specific Groq model if the built-in fallback list gets stale.
#   curl https://api.groq.com/openai/v1/models -H "Authorization: Bearer YOUR_KEY"
# VITE_GROQ_MODEL=llama-3.3-70b-versatile
