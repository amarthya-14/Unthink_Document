# Get a free key at https://console.groq.com/keys (no credit card required)
VITE_GROQ_API_KEY=your_groq_api_key_here

# Optional: force a specific model if the built-in fallback list gets stale.
# Run this to see exactly which models your key can access:
#   curl https://api.groq.com/openai/v1/models -H "Authorization: Bearer YOUR_KEY"
# VITE_GROQ_MODEL=llama-3.3-70b-versatile
